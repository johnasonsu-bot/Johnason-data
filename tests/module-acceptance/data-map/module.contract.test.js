const assert = require("node:assert/strict"); const test = require("node:test"); const candidate = require("../../../packages/data-platform-module-data-map");
test("data-map exposes all 41 baseline APIs and governance targets", () => { assert.equal(candidate.moduleManifest.sourceApiKeys.length, 41); assert.equal(candidate.createCapabilities({}).length, 41); assert.ok(candidate.createCapabilities({}).find((c) => c.capabilityId === "dataMap.analyzeResourceProfile").executionTargets.includes("api")); });
test("data-map delegates injected ports and fails closed", async () => { let got; const cs = candidate.createCapabilities({ service: { getOverview: async (input, context) => { got = [input, context]; return { ok: true }; } } }); assert.deepEqual(await cs[0].execute({ projectId: 2 }, { actor: { id: 1 } }), { ok: true }); assert.deepEqual(got, [{ projectId: 2 }, { actor: { id: 1 } }]); await assert.rejects(() => candidate.createRuntimeAdapters({}).getOverview({}), /not configured/i); });
