const assert = require("node:assert/strict"); const test = require("node:test"); const candidate = require("../../../packages/data-platform-module-service-runtime");
test("service-runtime covers both dynamic service APIs and four databases plus API", () => { assert.deepEqual(candidate.moduleManifest.sourceApiKeys, ["GET /api/service/*", "POST /api/service/*"]); assert.equal(candidate.createCapabilities({}).length, 2); assert.deepEqual(candidate.createCapabilities({})[0].executionTargets, ["mysql", "postgresql", "oracle", "dm", "api"]); });
test("service-runtime delegates the method and fails closed", async () => { let input; const cs = candidate.createCapabilities({ service: { handleInvoke: async (value, context) => { input = [value, context]; return { ok: true }; } } }); assert.deepEqual(await cs[0].execute({ path: "/health" }, { actor: { id: 1 } }), { ok: true }); assert.deepEqual(input, [{ path: "/health", method: "GET" }, { actor: { id: 1 } }]); await assert.rejects(() => candidate.createRuntimeAdapters({}).handleInvoke({}), /not configured/i); });
