const assert = require("node:assert/strict"); const test = require("node:test"); const candidate = require("../../../packages/data-platform-module-dev-ai-configs");
test("dev-ai-configs covers both baseline APIs and strict manifest", () => { assert.deepEqual(candidate.moduleManifest.sourceApiKeys, ["GET /api/v1/dev-ai-configs", "PUT /api/v1/dev-ai-configs/:id"]); assert.equal(candidate.createCapabilities({}).length, 2); assert.deepEqual(candidate.createCapabilities({})[0].executionTargets, ["mysql"]); });
test("dev-ai-configs delegates and fails closed", async () => { let input; const cs = candidate.createCapabilities({ service: { listConfigs: async (value, context) => { input = [value, context]; return []; } } }); assert.deepEqual(await cs[0].execute({ projectId: 1 }, { actor: { id: 2 } }), []); assert.deepEqual(input, [{ projectId: 1 }, { actor: { id: 2 } }]); await assert.rejects(() => candidate.createRuntimeAdapters({}).listConfigs({}), /not configured/i); });
