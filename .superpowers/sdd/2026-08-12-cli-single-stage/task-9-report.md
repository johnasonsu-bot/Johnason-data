# Foundation Task 9 report

Status: complete candidate; independent review round 2/5 Ready with 0 Critical and 0 Important findings.

## RED

- Installed E2E first failed because the test-only fake runtime did not exist.
- After adding the fixture, the real installed bin exposed missing `--version` behavior.
- Root help did not expose registry commands before default definitions were bound for help.
- The first repository-read audit could not distinguish module-loader escapes; a `require()` negative probe failed to produce the required `module escape` evidence before `_resolveFilename`/`_load` auditing was added.
- The original 8-line Skill failed independent retrieval/application/gap scenarios for profiles, auth, project, JSON exits, doctor, REPL, no-HTTP, and scope.

## GREEN

- Five real tarballs (kernel 0.1.0, auth 0.2.0, project-spaces 0.2.0, aggregate 0.1.0, CLI 0.1.0) install into a fresh prefix without workspace/file dependencies.
- The resolved installed bin runs from an arbitrary cwd and verifies discoverable help, version, invalid exit 2, and a single JSON error document.
- The preload rejects filesystem and non-builtin module paths outside the prefix, rejects listeners, and includes working read/require negative probes.
- Direct `main(argv, dependencies)` fake-runtime login/project resolve/use/access-check completes with no HTTP module, URL, listener, or environment injection backdoor.
- Real registry `sourceApiKeys` exist in the approved baseline. Historical project command-label drift remains unchanged; no key was invented.
- README and 387-word Skill document secure profiles/keychain/auth/project/JSON exits/REPL/doctor/no-HTTP/foundation scope; the updated Skill passed both independent scenario rounds and registry quick-reference validation.

## Fresh verification

- CLI: 66/66 passed; final full TAP is recorded in `packages/data-platform-cli/tests/TEST.md`.
- Installed/pack: five-tarball fresh install passed; CLI dry-pack contains 21 files (16.3 kB packed, 61.6 kB unpacked).
- Backend: 40 passed, 4 optional live-database tests skipped.
- Workspace: `npm run test:core` exited 0 after the review fix.
- Boundary: 7/7 passed.
- Coverage: regenerated byte-identically; 596/596 API routes and 84/84 frontend paths mapped.
- Syntax and `git diff --check`: exited 0.

## Concerns

- Minor: direct injected `main(["--help"|"--version"], dependencies)` leaves Commander help/version on process stdout instead of the injected stream. Installed behavior is correct; record for later output-binding cleanup.
- Foundation scope intentionally excludes durable jobs and later business-domain commands.
- Approved baseline still labels `GET /api/v1/projects/my` as `project list-my-projects`; the real registry command is `project list`. Align only in a future approved baseline regeneration.
- The pre-existing untracked `artifacts/` directory was not touched.
