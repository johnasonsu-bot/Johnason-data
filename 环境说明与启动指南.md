# AI智能数据中台源码版：环境说明与启动指南

本文档适用于第三方源码版的本地开发、功能体验和二次开发环境。源码包默认使用独立端口与容器，不会占用主项目的服务端口。

## 0. 源码技术组成

- 前端：React、TypeScript、Vite、Ant Design；
- 后端：Node.js、Express；
- 平台数据库：MySQL 8.4（Docker 容器运行）；
- 依赖管理：npm 与随包 `package-lock.json`；
- 容器编排：Docker Compose。

## 1. 环境依赖

| 项目 | 要求 | 用途 |
| --- | --- | --- |
| 操作系统 | Windows 10/11（推荐） | 随包快速启动与停止脚本使用 Windows PowerShell。 |
| PowerShell | 5.1 或更高 | 执行 `scripts/start-dev.ps1`、`scripts/stop-dev.ps1`。 |
| Node.js | 20.19+ 或 22 LTS（推荐） | 安装并运行前后端依赖。 |
| npm | 随 Node.js 安装 | 启动脚本通过 `npm.cmd ci` 安装依赖。 |
| Python | Python 3，命令默认为 `python` | DataX 任务启动脚本使用；可通过 `PYTHON` 指定可执行文件。 |
| Java | JDK 8、11 或 17 | DataX 数据同步任务运行时使用。 |
| Docker Desktop | 已启动，Docker Compose V2 可用 | 运行独立 MySQL 8.4 容器。 |
| 网络 | 首次启动需要访问 npm 镜像与 Docker 镜像仓库 | 下载 Node.js 依赖和 MySQL 镜像；已缓存后可减少网络依赖。 |

源码包内置的是平台自身的 MySQL 环境。需要接入 PostgreSQL、其他 MySQL、文件服务器、接口或消息系统时，由买方自行准备目标系统、网络连通和连接参数。

### 1.1 Oracle 与达梦 DataX 运行依赖

源码包内置 Oracle `oraclereader/oraclewriter`、达梦 `rdbmsreader/rdbmswriter`、Oracle JDBC 21.21 与达梦 JDBC 8.1.3.140。运行 Oracle 或达梦 DataX 同步前必须确保 `java -version` 可执行，推荐 JDK 8 或 JDK 11，并正确设置 `JAVA_HOME`。

可通过以下命令校验插件描述、插件主类和 JDBC Driver 类是否齐全：

```bash
node scripts/verify-datax-enterprise-plugins.js
```

需要从官方源码重新构建插件时执行：

```bash
scripts/install-datax-enterprise-plugins.sh
```

该脚本固定使用 Alibaba DataX 提交 `80ec23d5c5328eb90ca364d2749e92dfaf44541e`，通过 Maven JDK 8 容器编译，并对从 Maven Central 下载的 Oracle、达梦 JDBC 驱动执行 SHA-256 校验。脚本不需要、也不会写入任何数据库账号或密码。可通过 `DATAX_HOME` 指定目标 DataX 目录，通过 `DATAX_SOURCE_DIR` 复用已经校验到相同提交的 DataX 源码目录。

### 1.2 文档预览与 LibreOffice 运行依赖

知识库公共预览能力使用 LibreOffice 将 Word、Excel 和 PowerPoint 文档转换为 PDF。部署环境必须安装 LibreOffice，并确保 `soffice` 可执行；容器部署可直接使用源码内的 `backend/Dockerfile`，其中已经包含 Writer、Calc、Impress、中文字体和 Java 运行时。

本地环境可执行对应脚本安装依赖：

```bash
scripts/install-preview-dependencies.sh
```

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\install-preview-dependencies.ps1
```

如果 `soffice` 不在系统 `PATH` 中，可通过环境变量指定绝对路径：

```text
LIBREOFFICE_BIN=/absolute/path/to/soffice
```

Office 转换只在服务端临时缓存目录生成 PDF，不会覆盖知识库原文件。HTML 文件在独立沙箱中预览，并注入限制网络访问、跨窗口导航和插件执行的内容安全策略。

## 2. 默认地址、数据库与账号

| 类型 | 默认值 | 说明 |
| --- | --- | --- |
| 前端 | `http://127.0.0.1:46120` | 浏览器访问地址。 |
| 后端 | `http://127.0.0.1:46121` | 健康检查：`/api/health`。 |
| MySQL | `127.0.0.1:46122` | 容器名：`data-platform-source-mysql`。 |
| 数据库名 | `data_platform_source` | 平台自身业务库。 |
| MySQL 管理员 | `root` / `change-me` | 来自 `.env` 的本地开发默认值。 |
| 平台初始账号 | `admin` / `Admin@123` | 首次初始化后可登录平台。 |

以上密码仅适用于本地开发初始环境。首次启动前请修改 `.env` 中的 `DB_PASSWORD`、`JWT_SECRET`；如用于可被他人访问的环境，还必须登录平台修改初始管理员密码，并自行完成网络、安全和备份配置。

## 3. 配置文件与参数

首次运行时，启动脚本会将 `.env.example` 复制为根目录 `.env`，并同步复制到 `backend/.env`。可先复制并修改后再启动：

```powershell
Copy-Item .env.example .env
```

| 参数 | 默认值 | 作用 |
| --- | --- | --- |
| `NODE_ENV` | `development` | 运行环境标识。 |
| `PORT` | `46121` | 后端服务端口。 |
| `FRONTEND_URL` | `http://127.0.0.1:46120` | 后端允许的前端访问地址。 |
| `VITE_PROXY_TARGET` | `http://127.0.0.1:46121` | 前端开发代理目标。 |
| `DB_HOST` | `127.0.0.1` | 平台 MySQL 主机。 |
| `DB_PORT` | `46122` | 平台 MySQL 宿主机端口。 |
| `DB_USER` | `root` | 平台 MySQL 登录用户。 |
| `DB_PASSWORD` | `change-me` | 平台 MySQL root 密码；应修改。 |
| `DB_NAME` | `data_platform_source` | 平台 MySQL 数据库名。 |
| `JWT_SECRET` | `replace-with-a-random-secret` | 登录令牌签名密钥；必须替换为随机长字符串。 |
| `PYTHON` | `python` | DataX 使用的 Python 3 可执行命令或完整路径。 |
| `BACKGROUND_SCHEDULERS_ENABLED` | `false` | 是否启用后台调度；本地体验默认关闭。 |
| `SEED_DEMO_DATA` | `false` | 是否额外写入内置演示数据；源码包已通过独立快照初始化示例资产，默认关闭以避免重复。 |

`scripts/start-dev.ps1` 与 `compose.dev.yml` 固定使用 `46120`、`46121`、`46122` 及 `data_platform_source`。如确需修改端口或数据库名，须同步调整这两个文件及 `.env`，不能只改 `.env`。

## 4. 启动、停止与日志

在源码包根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start-dev.ps1
```

启动脚本会启动独立 MySQL、等待数据库就绪、安装前后端依赖、初始化数据库与项目资产，并在后台启动前后端服务。首次启动需要下载依赖并初始化示例资产，耗时通常高于后续启动；脚本只有在前后端健康检查通过后才会报告成功。日志位置：

- `runtime/local-dev/backend.log`
- `runtime/local-dev/backend-error.log`
- `runtime/local-dev/frontend.log`
- `runtime/local-dev/frontend-error.log`

停止本源码版环境：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\stop-dev.ps1
```

停止脚本会结束由当前源码包启动的前后端后台进程，并停止、移除 `data-platform-source-mysql` 容器；默认保留 MySQL 数据卷。如需从零初始化，请先确认数据已不再需要，再手动删除 Docker 卷 `data-platform-source-mysql-data`。删除数据卷会永久清除源码版数据库，不能作为日常停止步骤执行。

前端也可以在 `frontend` 目录直接执行 `npm.cmd run dev`，其固定监听地址仍为 `http://127.0.0.1:46120`。

源码包静态边界、依赖、后端测试和前端构建可通过以下命令统一验证：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\verify-package.ps1
```

## 5. 启动后检查

1. 浏览器打开 `http://127.0.0.1:46120`；
2. 使用 `admin / Admin@123` 登录；
3. 确认可见资产检索、数据接入、质量管控、数据开发、数据建模、数据服务、数据治理、报表平台和系统管理等目录；
4. 访问 `http://127.0.0.1:46121/api/health`，应返回健康状态；
5. 如启动失败，先查看上述日志，再执行停止脚本后重新启动。

## 6. 使用边界

本源码包用于买方内部学习、部署和二次开发。生产环境的服务器、域名、HTTPS、账号权限、数据安全、备份恢复、监控告警、性能调优、第三方数据源和合规要求均由买方自行负责。完整授权限制以同包《购买与授权协议》为准。
