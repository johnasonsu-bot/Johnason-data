const createTableStatements=[
`CREATE TABLE IF NOT EXISTS system_knowledge_base (id BIGINT PRIMARY KEY AUTO_INCREMENT,project_id BIGINT NULL,kb_name VARCHAR(128) NOT NULL,kb_desc VARCHAR(1024) NULL,tags_json JSON NULL,status VARCHAR(16) NOT NULL DEFAULT 'active',created_by VARCHAR(64) NOT NULL DEFAULT 'system',created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,KEY idx_system_kb_project(project_id))`,
`CREATE TABLE IF NOT EXISTS system_knowledge_base_document (id BIGINT PRIMARY KEY AUTO_INCREMENT,kb_id BIGINT NOT NULL,file_name VARCHAR(255) NOT NULL,file_type VARCHAR(32) NOT NULL,file_path VARCHAR(512) NOT NULL,file_size BIGINT NOT NULL DEFAULT 0,parse_status VARCHAR(16) NOT NULL DEFAULT 'WAIT_PARSE',parse_summary TEXT NULL,vector_status VARCHAR(16) NOT NULL DEFAULT 'PENDING',doc_status VARCHAR(16) NOT NULL DEFAULT 'active',chunk_count INT NOT NULL DEFAULT 0,last_parsed_at DATETIME NULL,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,KEY idx_system_kb_doc_kb(kb_id),CONSTRAINT fk_system_kb_doc_kb FOREIGN KEY(kb_id) REFERENCES system_knowledge_base(id) ON DELETE CASCADE)`,
`CREATE TABLE IF NOT EXISTS system_knowledge_base_chunk (id BIGINT PRIMARY KEY AUTO_INCREMENT,kb_doc_id BIGINT NOT NULL,kb_id BIGINT NOT NULL,chunk_index INT NOT NULL,content LONGTEXT NOT NULL,keywords_json JSON NULL,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,KEY idx_system_kb_chunk_doc(kb_doc_id),CONSTRAINT fk_system_kb_chunk_doc FOREIGN KEY(kb_doc_id) REFERENCES system_knowledge_base_document(id) ON DELETE CASCADE,CONSTRAINT fk_system_kb_chunk_kb FOREIGN KEY(kb_id) REFERENCES system_knowledge_base(id) ON DELETE CASCADE)`
];
module.exports={createTableStatements};
